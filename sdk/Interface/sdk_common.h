#ifndef _SDK_COMMON_H
#define _SDK_COMMON_H

typedef enum {
    SDK_FAILURE = -1,
    SDK_SUCCESS = 0,
    SDK_INVALID_PARAMS,
    SDK_BUSY,
    SDK_TIMEOUT,
    SDK_MUTEX_NOT_OWNED,
    SDK_INVALID_STATE,
    SDK_NOT_FOUND

}SDK_STAT;

#endif //_SDK_COMMON_H